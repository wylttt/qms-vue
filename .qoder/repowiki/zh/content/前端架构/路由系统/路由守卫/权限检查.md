# 权限检查

<cite>
**本文档引用的文件**   
- [hasRole.js](file://bear-jia-vue3/src/directive/permission/hasRole.js)
- [hasPermi.js](file://bear-jia-vue3/src/directive/permission/hasPermi.js)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js)
- [user.js](file://bear-jia-vue3/src/stores/user.js)
- [vueRouter.js](file://bear-jia-vue3/src/router/vueRouter.js)
- [routes.js](file://bear-jia-vue3/src/router/routes.js)
- [auth.js](file://bear-jia-vue3/src/plugins/auth.js)
- [index.js](file://bear-jia-vue3/src/directive/index.js)
</cite>

## 目录
1. [权限检查机制概述](#权限检查机制概述)
2. [核心组件分析](#核心组件分析)
3. [路由守卫与动态路由生成](#路由守卫与动态路由生成)
4. [权限验证逻辑](#权限验证逻辑)
5. [权限指令实现](#权限指令实现)
6. [权限配置与使用示例](#权限配置与使用示例)
7. [常见问题排查](#常见问题排查)

## 权限检查机制概述

本系统采用基于角色（Role-Based）和操作权限（Permission-Based）的双重权限控制机制，通过Vue Router路由守卫、Pinia状态管理、自定义指令等技术实现完整的权限管理体系。系统在用户登录后获取角色和权限信息，动态生成路由表，并在路由跳转和元素渲染时进行权限验证。

**Section sources**
- [vueRouter.js](file://bear-jia-vue3/src/router/vueRouter.js#L1-L130)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L1-L264)

## 核心组件分析

系统权限管理由多个核心组件协同工作，包括用户存储（userStore）、权限存储（permissionStore）、路由管理器和权限验证服务。

```mermaid
graph TB
subgraph "权限管理核心组件"
UserStore["userStore<br>用户信息存储"]
PermissionStore["permissionStore<br>权限路由存储"]
Router["Vue Router<br>路由管理"]
AuthPlugin["auth插件<br>权限验证服务"]
end
UserStore --> |提供角色和权限数据| PermissionStore
UserStore --> |提供角色和权限数据| AuthPlugin
PermissionStore --> |动态添加路由| Router
AuthPlugin --> |权限验证| Router
AuthPlugin --> |权限验证| 指令系统
subgraph "权限指令"
HasRole["v-hasRole<br>角色权限指令"]
HasPermi["v-hasPermi<br>操作权限指令"]
end
AuthPlugin --> HasRole
AuthPlugin --> HasPermi
```

**Diagram sources **
- [user.js](file://bear-jia-vue3/src/stores/user.js#L1-L83)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L1-L264)
- [auth.js](file://bear-jia-vue3/src/plugins/auth.js#L1-L61)

**Section sources**
- [user.js](file://bear-jia-vue3/src/stores/user.js#L1-L83)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L1-L264)

## 路由守卫与动态路由生成

系统通过路由守卫实现权限控制，结合userStore和permissionStore实现动态路由权限控制。当用户访问受保护的路由时，系统会检查用户身份和权限，并动态生成相应的路由表。

### generateRoutes() 方法流程

`generateRoutes()` 方法负责从后端获取路由数据、过滤异步路由并动态添加到Vue Router的完整流程：

```mermaid
sequenceDiagram
participant 用户
participant 路由守卫 as 路由守卫
participant userStore as userStore
participant permissionStore as permissionStore
participant 后端 as 后端API
用户->>路由守卫 : 访问受保护路由
路由守卫->>userStore : 检查token存在
alt 用户已登录
路由守卫->>userStore : 检查用户角色信息
alt 角色信息为空
userStore->>后端 : 调用getInfo()获取用户信息
后端-->>userStore : 返回用户角色和权限
userStore->>permissionStore : 调用generateRoutes()
permissionStore->>后端 : 调用getRouters()获取路由数据
后端-->>permissionStore : 返回路由数据
permissionStore->>permissionStore : filterAsyncRouter过滤外链
permissionStore->>permissionStore : filterAsyncRouterForSidebar处理侧边栏
permissionStore->>VueRouter : 动态添加路由
permissionStore-->>路由守卫 : 返回生成的路由
路由守卫->>用户 : 允许访问
else 角色信息已存在
路由守卫->>VueRouter : 检查路由是否存在
alt 路由不存在
路由守卫->>permissionStore : 重新生成路由
permissionStore-->>路由守卫 : 返回生成的路由
路由守卫->>用户 : 允许访问
else 路由存在
路由守卫->>用户 : 允许访问
end
end
else 用户未登录
路由守卫->>用户 : 重定向到登录页
end
```

**Diagram sources **
- [vueRouter.js](file://bear-jia-vue3/src/router/vueRouter.js#L38-L97)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L234-L259)

**Section sources**
- [vueRouter.js](file://bear-jia-vue3/src/router/vueRouter.js#L38-L97)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L234-L259)

## 权限验证逻辑

系统实现了基于角色和操作权限的双重验证机制，确保不同层级的访问控制需求。

### hasPermission() 函数

`hasPermission()` 函数基于角色（roles）实现路由访问控制逻辑：

```mermaid
flowchart TD
Start([开始]) --> CheckMeta{"路由是否有meta.roles?"}
CheckMeta --> |是| CheckRole["检查用户角色是否在meta.roles中"]
CheckRole --> Result1{"匹配成功?"}
Result1 --> |是| Allow["允许访问"]
Result1 --> |否| Deny["拒绝访问"]
CheckMeta --> |否| AllowDefault["无角色限制<br>允许访问"]
Allow --> End([结束])
Deny --> End
AllowDefault --> End
```

**Diagram sources **
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L10-L15)

### filterDynamicRoutes() 函数

`filterDynamicRoutes()` 函数基于操作权限（permissions）实现验证机制：

```mermaid
flowchart TD
Start([开始]) --> LoopRoutes["遍历路由数组"]
LoopRoutes --> CheckPermissions{"路由有permissions属性?"}
CheckPermissions --> |是| CheckPermiOr["验证hasPermiOr权限"]
CheckPermiOr --> Result1{"权限验证通过?"}
Result1 --> |是| AddRoute["添加到结果数组"]
Result1 --> |否| SkipRoute["跳过该路由"]
CheckPermissions --> |否| CheckRoles{"路由有roles属性?"}
CheckRoles --> |是| CheckRoleOr["验证hasRoleOr权限"]
CheckRoleOr --> Result2{"角色验证通过?"}
Result2 --> |是| AddRoute
Result2 --> |否| SkipRoute
CheckRoles --> |否| AddRoute
AddRoute --> NextRoute["处理下一个路由"]
SkipRoute --> NextRoute
NextRoute --> MoreRoutes{"还有更多路由?"}
MoreRoutes --> |是| LoopRoutes
MoreRoutes --> |否| ReturnResult["返回过滤后的路由数组"]
ReturnResult --> End([结束])
```

**Diagram sources **
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L194-L207)

**Section sources**
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L10-L36)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L194-L207)

## 权限指令实现

系统提供了两种权限指令：`v-hasRole` 和 `v-hasPermi`，用于在模板中直接控制元素的显示权限。

### 指令注册流程

```mermaid
flowchart TD
A["main.js"] --> B["导入directive"]
B --> C["app.use(directive)"]
C --> D["执行directive/index.js"]
D --> E["install函数"]
E --> F["注册v-hasPermi指令"]
F --> G["Vue.directive('hasPermi', hasPermi)"]
G --> H["指令注册完成"]
```

**Diagram sources **
- [main.js](file://bear-jia-vue3/src/main.js#L6-L7)
- [index.js](file://bear-jia-vue3/src/directive/index.js#L8-L11)

### v-hasRole 指令实现

`v-hasRole` 指令的实现原理：

```mermaid
flowchart TD
Start([指令挂载]) --> GetBinding["获取binding.value"]
GetBinding --> CheckValue{"值有效?"}
CheckValue --> |否| ThrowError["抛出错误"]
CheckValue --> |是| GetRoles["从store获取用户角色"]
GetRoles --> CheckAdmin["检查是否为admin超级管理员"]
CheckAdmin --> |是| ShowElement["显示元素"]
CheckAdmin --> |否| CheckInclude["检查角色是否包含在value中"]
CheckInclude --> |是| ShowElement
CheckInclude --> |否| RemoveElement["移除元素"]
ShowElement --> End([结束])
RemoveElement --> End
ThrowError --> End
```

**Diagram sources **
- [hasRole.js](file://bear-jia-vue3/src/directive/permission/hasRole.js#L9-L28)

### v-hasPermi 指令实现

`v-hasPermi` 指令的实现原理：

```mermaid
flowchart TD
Start([指令挂载]) --> GetBinding["获取binding.value"]
GetBinding --> CheckValue{"值为空?"}
CheckValue --> |是| ShowDefault["默认显示"]
CheckValue --> |否| CheckArray{"值为数组?"}
CheckArray --> |否| ThrowError["抛出错误"]
CheckArray --> |是| GetPermissions["从userStore获取用户权限"]
GetPermissions --> CheckAll["检查是否为*:*:*超级权限"]
CheckAll --> |是| ShowElement["显示元素"]
CheckAll --> |否| CheckInclude["检查权限是否包含在value中"]
CheckInclude --> |是| ShowElement
CheckInclude --> |否| RemoveElement["移除元素"]
ShowElement --> End([结束])
RemoveElement --> End
ShowDefault --> End
ThrowError --> End
```

**Diagram sources **
- [hasPermi.js](file://bear-jia-vue3/src/directive/permission/hasPermi.js#L9-L34)

**Section sources**
- [hasRole.js](file://bear-jia-vue3/src/directive/permission/hasRole.js#L1-L28)
- [hasPermi.js](file://bear-jia-vue3/src/directive/permission/hasPermi.js#L1-L34)

## 权限配置与使用示例

### 使用场景

| 指令 | 使用场景 | 示例 |
|------|--------|------|
| **v-hasRole** | 基于用户角色的访问控制 | 只有管理员角色才能看到系统设置菜单 |
| **v-hasPermi** | 基于具体操作权限的访问控制 | 只有拥有"system:user:add"权限的用户才能看到新增按钮 |

### 实际使用示例

```vue
<template>
  <!-- 基于操作权限的按钮控制 -->
  <a-button 
    v-hasPermi="['monitor:job:add']" 
    type="primary" 
    @click="openJobAddModal">
    新增任务
  </a-button>
  
  <!-- 基于角色的菜单项控制 -->
  <a-menu-item 
    v-hasRole="['admin', 'system']" 
    key="system-config">
    系统配置
  </a-menu-item>
  
  <!-- 多权限"或"关系控制 -->
  <a-button 
    v-hasPermi="['system:user:edit', 'system:user:add']" 
    type="primary">
    编辑/新增用户
  </a-button>
</template>
```

**Section sources**
- [index.vue](file://bear-jia-vue3/src/views/monitor/job/index.vue#L13-L20)
- [index.vue](file://bear-jia-vue3/src/views/system/config/index.vue#L13-L20)

## 常见问题排查

### 权限配置错误排查

```mermaid
flowchart TD
Start([问题: 权限不生效]) --> CheckToken["检查用户token"]
CheckToken --> |无效| Login["重新登录获取token"]
CheckToken --> |有效| CheckUserInfo["检查用户信息获取"]
CheckUserInfo --> |失败| APIError["检查getInfo接口"]
CheckUserInfo --> |成功| CheckRoutes["检查路由生成"]
CheckRoutes --> |失败| GenerateError["检查generateRoutes流程"]
CheckRoutes --> |成功| CheckDirective["检查指令使用"]
CheckDirective --> CheckSyntax["检查指令语法是否正确"]
CheckSyntax --> |错误| FixSyntax["修正语法"]
CheckSyntax --> |正确| CheckPermission["检查权限字符串是否匹配"]
CheckPermission --> |不匹配| FixPermission["修正权限字符串"]
CheckPermission --> |匹配| CheckElement["检查元素是否被正确移除"]
CheckElement --> |未移除| Debug["使用浏览器开发者工具调试"]
CheckElement --> |已移除| Solved["问题解决"]
```

### 路由无法访问问题排查

1. **检查token有效性**：确保用户已登录且token未过期
2. **检查用户角色**：确认用户角色是否包含访问该路由所需的角色
3. **检查路由配置**：确认路由的meta.roles配置是否正确
4. **检查网络请求**：查看getRouters接口是否成功返回路由数据
5. **检查控制台错误**：查看是否有JavaScript错误影响权限判断

**Section sources**
- [vueRouter.js](file://bear-jia-vue3/src/router/vueRouter.js#L58-L66)
- [permission.js](file://bear-jia-vue3/src/stores/permission.js#L253-L255)